# 12 Next Steps
1. **防護網建立 (P4)**: 為 `SpcEngine` (Calculators 與 NelsonRulesValidator) 建立 xUnit 單元測試，確保核心計算邏輯的正確性。
2. **Formula Engine 重構 (P2)**: 將 `FormulaEngineService` 進行升級，考慮引入 `NCalc` 實作動態解析與版本化管理。
3. **API 完善與前端對接**: 確認前端對於新加入的 `violatedRules` 等欄位的支援與顯示。
