# 舊藥液資料匯入整理（測試區）

日期：2026-08-31  
目標環境：SPC 測試區（API 8081）

## 原則

- 現行啟用中的 CHEM PPC（線別、槽位、分析項目、單位、規格）是唯一主檔依據。
- 舊檔的規格與範圍只協助辨識來源組合，不得回寫或覆蓋現行 PPC 規格。
- 對應鍵使用「線別 + 舊槽位 + 舊分析項目 + 單位 + 舊規格範圍」，避免同名分析項目被全域誤換。
- 只有唯一且高信心的對應可自動進入預覽；歧義、找不到現行 PPC、缺量測值者一律隔離。
- 同一 PPC 與量測時間有多筆舊紀錄時，以遞增 `SampleNo` 保留各筆資料。

## 資料盤點與預覽結果

| 分類 | 筆數 | 處理方式 |
|---|---:|---|
| 原始藥液資料 | 12,908 | 2026-01 至 2026-08，共 8 檔 |
| 唯一高信心對應 | 7,920 | 已建立測試區預覽 |
| 需人工判定 | 3,635 | 不匯入，等待槽位／項目確認 |
| 無現行可用對應 | 1,256 | 不匯入，不自動建立舊主檔 |
| 缺量測值 | 97 | 不匯入 |

預覽分成 40 批（每批 200 筆，末批 120 筆）：

- Total：7,920
- Valid：7,920
- Error：0
- Request failed：0
- 狀態：全部 `PreviewReady`，尚未呼叫 confirm

完整轉換輸出、對應決策與批次編號保存在本機 `TransFiles/legacy_chemical_test_ready`，不納入 Git，避免將實際量測資料上傳到程式碼倉庫。

## 程式保護修正

`UploadService.ConfirmAsync` 不再用匯入列的「規格／範圍」修改 PPC 的 `TargetValue`、`LSL`、`USL`。量測匯入只處理量測資料；現行管制規格須由主檔維護流程管理。

## 後續處理

1. 取得預覽筆數確認後，才對 40 個批次執行 `confirm`。
2. confirm 後逐批核對 Imported 狀態與實際新增／更新筆數。
3. 3,635 筆人工判定資料須由使用者確認歧義對應，再產生第二輪預覽。
4. 1,256 筆無現行對應資料，除非先由主檔管理流程正式建立現行 PPC，否則維持不匯入。
